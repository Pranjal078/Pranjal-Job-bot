# Job Search Alert Bot — Python Dependencies
# Requires Python 3.11+  (tested on 3.11, 3.12, 3.13)
# Install with: pip install -r requirements.txt

# HTTP
requests==2.32.3
urllib3==2.2.3

# RSS/Atom feed parsing
feedparser==6.0.11

# YAML config parsing
PyYAML==6.0.2

# Telegram notifications
python-telegram-bot==21.5

# Gmail API (for LinkedIn email parsing)
google-api-python-client==2.143.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# HTML parsing (for email body and career page fallbacks)
beautifulsoup4==4.12.3
lxml==5.3.0

# Date parsing (robust handling of varied date formats from feeds)
python-dateutil==2.9.0.post0

# Environment variables
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
responses==0.25.3   # HTTP mock for tests
